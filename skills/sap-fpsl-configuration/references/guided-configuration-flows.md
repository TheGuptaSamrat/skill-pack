# Guided Configuration Flows

- Break configuration help into small ordered steps.
- Separate:
  - prerequisites
  - setup actions
  - validation checks
  - downstream dependency checks
- Make it explicit which steps are confirmed by evidence and which are generic guidance.
