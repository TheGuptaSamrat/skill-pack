# CVPM Validation

- confirm the business event or period-end requirement that should trigger the CVPM run
- verify prerequisites, input evidence, and downstream checkpoints before claiming the design is ready
- include validation points for process-step sequencing, method choice, result visibility, and operational review
- use placeholders for run control artifacts when customer-specific evidence is missing
- route SQL-based result validation to `reconciliation`
