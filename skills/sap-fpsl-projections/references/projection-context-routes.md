# Projection Context Routes

Use the smallest relevant context set for estimation work.

- Load `../../../docs-context/training/fpsl/projection-and-sizing-concepts.md` for curated projection logic and sensitivity framing.
- Load `../../../metadata-drop/normalized/` for active record-shape, table, and sizing inputs when they exist.
- Load `../../../metadata-drop/raw-excel/` only when normalized sizing inputs are missing or being compared.
- Do not load full-source markdown by default; use `../../../docs-context/full-source-md/` only for deeper source review.
