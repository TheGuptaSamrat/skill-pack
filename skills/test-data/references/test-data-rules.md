# Test Data Rules

- generate the smallest scenario-valid data set first
- populate mandatory attributes before optional enrichments
- keep synthetic values and assumptions clearly labeled
- prefer deterministic fixtures over random-looking data
- include negative and edge-case rows only when the scenario needs them
- include expected validation outcomes with the data set
