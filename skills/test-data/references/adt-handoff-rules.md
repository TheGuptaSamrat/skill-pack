# ADT Handoff Rules

Return one artifact block at a time in this shape:

1. `Artifact`
2. `Paste target`
3. `Action`
4. one fenced code block only
5. `Checks`

Use `SQL Console worksheet`, `local test class`, or `fixture helper class` as the usual paste targets.
