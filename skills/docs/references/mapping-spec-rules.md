# Mapping Spec Rules

- build mapping specifications when the task is source-to-target design
- separate confirmed metadata from inferred relationships
- keep confirmed, inferred, and unresolved sections explicit
- use mapping completeness checks when validating source coverage
- keep onboarding assumptions visible when source-system setup is incomplete
- do not fabricate field lists, joins, object names, or mappings
- label inferred relationships explicitly
