# Reconciliation Query Rules

- check counts, keys, totals, and status markers before deeper logic
- tie every query to a business checkpoint in the flow
- prefer DDIC-aware joins and predicates
- keep SQL narrow, staged, and readable
- label inferred checks when exact metadata is missing
- use placeholders instead of guessed fields or tables
