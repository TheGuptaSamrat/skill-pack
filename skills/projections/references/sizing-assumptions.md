# Sizing Assumptions

- Monthly volume may differ by process type or product line.
- Storage growth depends on:
  - record count
  - retained history
  - average row size
  - indexes and overhead if known
- If no storage basis is given, state a simplifying assumption rather than implying exactness.
