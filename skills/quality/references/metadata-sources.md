# Metadata Sources

Use these evidence sources first:

- DDIC tables, fields, keys, domains, and data elements
- staging, SDL, or RDL field layouts when provided
- CDS definitions if they shape quality expectations
- mapping notes when cross-field consistency depends on transformation logic
- sample source rows only as supporting evidence
