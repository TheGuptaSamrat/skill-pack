# Quality Rules

- generate completeness, key, null, domain, type, and cross-field checks from metadata evidence
- build confirmed rules first and inferred rules second
- tie each rule to a layer or checkpoint when possible
- prefer explicit field-level checks over vague quality statements
- keep placeholders visible when metadata is incomplete
- do not invent fields, domains, or rule semantics
