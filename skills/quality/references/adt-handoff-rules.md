# ADT Handoff Rules

Return one artifact block at a time in this shape:

1. `Artifact`
2. `Paste target`
3. `Action`
4. one fenced code block only
5. `Checks`

Use `SQL Console worksheet` or `rule-spec note` as the usual paste targets.
