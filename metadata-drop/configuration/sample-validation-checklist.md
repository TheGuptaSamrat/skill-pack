# Sample Configuration Validation Checklist

- inspect setup evidence
- confirm dependent master data exists
- validate source and target record counts
- review `SLG1` logs if available
- review scheduled jobs in `SM37` if processing is batch-driven
- confirm no unexpected posting or reconciliation variance
