# CDS Inputs

Place CDS DDL source or exported definitions here.

Label each file as one of:

- `reference-derived`
- `synthetic-example`

Recommended formats:

- plain `.ddl.txt`
- compact markdown notes
- CSV inventories of entities and fields
