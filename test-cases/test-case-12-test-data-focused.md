# Test Case 12: Focused Test Data

Skill:

- `test-data`

Scenario:

- generate a small but meaningful synthetic data set and insert-script approach for a batch-style functional scenario that needs FPSL/FSDM-required attributes populated

Use this metadata:

- `metadata-drop/ddic/current/fields-template.csv`
- `metadata-drop/samples/cashflow_rows.sample.csv`
- `metadata-drop/fpsl/core-structures.md`

Expected evaluation points:

- synthetic values are clearly labeled
- required attributes are populated
- scenario assumptions are explicit
- validation expectations are included
